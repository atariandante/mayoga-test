import { NextPage } from 'next';

// Components
import { Layout } from '../../components/Layout';

const New: NextPage = () => {
    return (
        <Layout>
            New page, ma maan!!
        </Layout>
    );
}

export default New;
